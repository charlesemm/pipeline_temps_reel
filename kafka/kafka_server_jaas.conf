// Etape 7e : JAAS cote broker. Les utilisateurs SCRAM sont stockes dans le
// journal de metadonnees KRaft (crees par scripts/kafka-secure-setup.ps1) :
// le broker n'a donc pas de mot de passe a porter ici.
KafkaServer {
    org.apache.kafka.common.security.scram.ScramLoginModule required;
};
